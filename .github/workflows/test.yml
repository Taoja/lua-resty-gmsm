name: Test::Nginx

on:
  push:
    branches: [main, master]
  pull_request:
  # 每周保活：GitHub 会删除 7 天未被访问的缓存条目，定时任务防止全矩阵缓存过期
  schedule:
    - cron: '0 3 * * 1'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # 默认分支上不取消：job 被 cancel 时 actions/cache 的 post 保存不会执行，
  # 每次 push 互相取消 = 缓存永远建立不起来
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' && github.ref != 'refs/heads/master' }}

permissions:
  contents: read

jobs:
  test:
    name: "OpenSSL ${{ matrix.ssl }} + OpenResty ${{ matrix.or }}"
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    continue-on-error: ${{ matrix.experimental }}

    strategy:
      fail-fast: false
      matrix:
        include:
          # ── tag 规则不一致，必须显式写死 ──
          #   1.1.1 系列：tag = OpenSSL_1_1_1w ，tarball = openssl-1.1.1w.tar.gz
          #   3.x/4.x ：tag = openssl-3.6.4     ，tarball = openssl-3.6.4.tar.gz
          - ssl: "1.1.1w"
            ssl_tag: "OpenSSL_1_1_1w"
            or: "1.25.3.2"
            experimental: false

          - ssl: "3.0.22"
            ssl_tag: "openssl-3.0.22"
            or: "1.25.3.2"
            experimental: false

          - ssl: "3.6.4"
            ssl_tag: "openssl-3.6.4"
            or: "1.25.3.2"
            experimental: false

          - ssl: "4.0.2"
            ssl_tag: "openssl-4.0.2"
            or: "1.31.1.1"
            experimental: true

    env:
      OPENSSL_VERSION:   ${{ matrix.ssl }}
      OPENSSL_TAG:       ${{ matrix.ssl_tag }}
      OPENRESTY_VERSION: ${{ matrix.or }}
      OPENSSL_PREFIX:   "/home/runner/opt/openssl-${{ matrix.ssl }}"
      OPENRESTY_PREFIX: "/home/runner/opt/openresty-${{ matrix.or }}-ssl${{ matrix.ssl }}"

    steps:
      - uses: actions/checkout@v4

      - name: Install build deps
        run: |
          set -eux
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential ca-certificates curl perl \
            cpanminus libipc-run-perl \
            libpcre3-dev libpcre2-dev zlib1g-dev
          gcc --version
          perl -v | head -2

      # ================= OpenSSL =================
      # restore / save 拆开写，好处：
      #   1. 可以用 outcome 精确控制"只在构建成功时保存"，避免写入半成品
      #   2. 测试失败时仍能保住已编译好的依赖（save-always 语义）
      - name: Restore OpenSSL
        id: ssl-cache
        uses: actions/cache/restore@v4
        with:
          path: /home/runner/opt/openssl-${{ matrix.ssl }}
          key: ssl-${{ runner.os }}-${{ matrix.ssl }}-shared

      - name: Build shared OpenSSL
        id: build-ssl
        if: steps.ssl-cache.outputs.cache-hit != 'true'
        run: |
          set -eux
          mkdir -p "${RUNNER_TEMP}/ssl-build"
          cd "${RUNNER_TEMP}/ssl-build"

          case "$OPENSSL_VERSION" in
            1.*) SSL_SERIES="1.1.1" ;;
            *)   SSL_SERIES="$(echo "$OPENSSL_VERSION" | cut -d. -f1-2)" ;;
          esac

          ok=0
          for url in \
            "https://github.com/openssl/openssl/releases/download/${OPENSSL_TAG}/openssl-${OPENSSL_VERSION}.tar.gz" \
            "https://www.openssl.org/source/openssl-${OPENSSL_VERSION}.tar.gz" \
            "https://www.openssl.org/source/old/${SSL_SERIES}/openssl-${OPENSSL_VERSION}.tar.gz"
          do
            echo "::group::try $url"
            if curl -fL --retry 3 --retry-delay 5 --max-time 300 \
                 -A "curl/8 GitHub-Actions" "$url" -o openssl.tar.gz; then
              ok=1; echo "::endgroup::"; break
            fi
            echo "::endgroup::"
          done
          [ "$ok" = 1 ] || { echo "::error::找不到 OpenSSL ${OPENSSL_VERSION} 的源码包"; exit 1; }

          tar -xzf openssl.tar.gz
          cd "openssl-${OPENSSL_VERSION}"

          # 1.1.1 必须小写 ./config（大写 Configure 强制要求显式 target）；
          # 1.1.1 无 --libdir，且不认识 -Wl,--enable-new-dtags
          case "$OPENSSL_VERSION" in
            1.*)
              ./config \
                --prefix="$OPENSSL_PREFIX" \
                --openssldir="$OPENSSL_PREFIX/ssl" \
                shared \
                '-Wl,-rpath,$(LIBRPATH)'
              ;;
            *)
              ./Configure \
                --prefix="$OPENSSL_PREFIX" \
                --openssldir="$OPENSSL_PREFIX/ssl" \
                --libdir=lib \
                shared \
                '-Wl,-rpath,$(LIBRPATH)' \
                -Wl,--enable-new-dtags
              ;;
          esac

          make -j"$(nproc)"
          if ! make install_sw install_ssldirs; then
            echo "::warning::install_ssldirs 不可用，退回 install_sw"
            make install_sw
          fi

          # ---- 瘦身：静态库 / pkgconfig / man / doc 一律不需要，.so 去符号表 ----
          rm -rf "$OPENSSL_PREFIX/lib/pkgconfig" \
                 "$OPENSSL_PREFIX/share/man" \
                 "$OPENSSL_PREFIX/share/doc" \
                 "$OPENSSL_PREFIX/html"
          rm -f  "$OPENSSL_PREFIX/lib/"*.a
          find "$OPENSSL_PREFIX/lib" -name '*.so*' -type f \
            -exec strip --strip-unneeded {} + 2>/dev/null || true
          strip --strip-unneeded "$OPENSSL_PREFIX/bin/openssl" 2>/dev/null || true

      - name: Save OpenSSL
        # outcome 判断保证只存完整产物；always() 保证后续测试失败也不丢缓存
        if: always() && steps.build-ssl.outcome == 'success'
        uses: actions/cache/save@v4
        with:
          path: /home/runner/opt/openssl-${{ matrix.ssl }}
          key: ssl-${{ runner.os }}-${{ matrix.ssl }}-shared

      # 无论是否命中缓存都做硬校验（缓存内容可能是坏的）
      - name: Assert OpenSSL build
        run: |
          set -eux
          "$OPENSSL_PREFIX/bin/openssl" version

          LIBCRYPTO="$(ls "$OPENSSL_PREFIX"/lib/libcrypto.so.* 2>/dev/null | head -1)"
          [ -n "$LIBCRYPTO" ] || { echo "::error::未找到 libcrypto.so.*"; ls -l "$OPENSSL_PREFIX/lib"; exit 1; }
          echo "libcrypto = $LIBCRYPTO"

          readelf -d "$LIBCRYPTO"                  | grep -E 'RPATH|RUNPATH' \
            || echo "::warning::libcrypto 无 RPATH/RUNPATH（对 1.1.1 可接受）"
          readelf -d "$OPENSSL_PREFIX/bin/openssl" | grep -E 'RPATH|RUNPATH' \
            || echo "::warning::openssl 可执行文件无 RPATH/RUNPATH"

          LD_LIBRARY_PATH="$OPENSSL_PREFIX/lib" ldd "$OPENSSL_PREFIX/bin/openssl" \
            | grep -E 'libcrypto|libssl' | grep -q "$OPENSSL_PREFIX" \
            || { echo "::error::openssl 仍链到系统库"; \
                 LD_LIBRARY_PATH="$OPENSSL_PREFIX/lib" ldd "$OPENSSL_PREFIX/bin/openssl"; exit 1; }

      # ================= OpenResty =================
      # 单独一个 key：与 OpenSSL 分离是安全的，因为 prefix 路径由 matrix 固定，
      # 即使 OpenSSL 缓存被驱逐而本缓存命中，重建 OpenSSL 后 rpath 依然指向同一路径
      - name: Restore OpenResty
        id: or-cache
        uses: actions/cache/restore@v4
        with:
          path: /home/runner/opt/openresty-${{ matrix.or }}-ssl${{ matrix.ssl }}
          key: or-${{ runner.os }}-${{ matrix.or }}-ssl${{ matrix.ssl }}-shared

      - name: Build OpenResty
        id: build-or
        if: steps.or-cache.outputs.cache-hit != 'true'
        run: |
          set -eux
          mkdir -p "${RUNNER_TEMP}/or-build"
          cd "${RUNNER_TEMP}/or-build"

          curl -fL --retry 3 --retry-delay 5 --max-time 300 \
            "https://openresty.org/download/openresty-${OPENRESTY_VERSION}.tar.gz" \
            -o openresty.tar.gz
          tar -xzf openresty.tar.gz
          cd "openresty-${OPENRESTY_VERSION}"

          # OpenResty 1.31 起内核自带 PCRE2，--with-pcre-jit 可能不再存在，
          # nginx configure 遇到未知选项会直接 error 退出，所以先探测
          PCRE_OPT=""
          if ./configure --help 2>&1 | grep -q 'with-pcre2-jit'; then
            PCRE_OPT="--with-pcre2-jit"
          elif ./configure --help 2>&1 | grep -q 'with-pcre-jit'; then
            PCRE_OPT="--with-pcre-jit"
          fi
          echo "PCRE_OPT=$PCRE_OPT"

          # 不用 --with-openssl（静态链接，FFI 够不着）；
          # 改用 -I / -L 指向已安装的共享库，并把 rpath 编进 nginx
          ./configure \
            --prefix="$OPENRESTY_PREFIX" \
            $PCRE_OPT \
            --with-http_ssl_module \
            --with-http_v2_module \
            --with-http_realip_module \
            --with-luajit \
            --with-stream \
            --with-stream_ssl_module \
            --with-cc-opt="-I$OPENSSL_PREFIX/include" \
            --with-ld-opt="-L$OPENSSL_PREFIX/lib -Wl,-rpath,$OPENSSL_PREFIX/lib -Wl,--enable-new-dtags"

          make -j"$(nproc)"
          make install

          # ---- 瘦身：luarocks / pod / man / COPYRIGHT 都不参与测试 ----
          rm -rf "$OPENRESTY_PREFIX/luarocks" \
                 "$OPENRESTY_PREFIX/pod" \
                 "$OPENRESTY_PREFIX/site/pod" \
                 "$OPENRESTY_PREFIX/site/man" \
                 "$OPENRESTY_PREFIX/COPYRIGHT" \
                 "$OPENRESTY_PREFIX/README" \
                 "$OPENRESTY_PREFIX/bin/restydoc" \
                 "$OPENRESTY_PREFIX/bin/restydoc-index" \
                 "$OPENRESTY_PREFIX/bin/md2pod.pl" \
                 "$OPENRESTY_PREFIX/bin/nginx-xml2pod"
          find "$OPENRESTY_PREFIX" -name '*.pod' -delete 2>/dev/null || true
          find "$OPENRESTY_PREFIX/nginx/sbin" "$OPENRESTY_PREFIX/luajit/bin" \
            "$OPENRESTY_PREFIX/bin" -type f \
            -exec strip --strip-unneeded {} + 2>/dev/null || true

      - name: Save OpenResty
        if: always() && steps.build-or.outcome == 'success'
        uses: actions/cache/save@v4
        with:
          path: /home/runner/opt/openresty-${{ matrix.or }}-ssl${{ matrix.ssl }}
          key: or-${{ runner.os }}-${{ matrix.or }}-ssl${{ matrix.ssl }}-shared

      - name: Assert OpenResty linkage
        run: |
          set -eux
          "$OPENRESTY_PREFIX/nginx/sbin/nginx" -V
          "$OPENRESTY_PREFIX/nginx/sbin/nginx" -V 2>&1 \
            | grep -q "OpenSSL ${OPENSSL_VERSION}" \
            || { echo "::error::nginx 未链接 OpenSSL ${OPENSSL_VERSION}"; exit 1; }
          ldd "$OPENRESTY_PREFIX/nginx/sbin/nginx" | grep -E 'libcrypto|libssl' \
            | grep -q "$OPENSSL_PREFIX" \
            || { echo "::error::nginx 的 libcrypto 未指向 $OPENSSL_PREFIX"; \
                 ldd "$OPENRESTY_PREFIX/nginx/sbin/nginx" | grep -E 'libcrypto|libssl'; exit 1; }

      # ================= Test::Nginx =================
      # 4 个组合共享同一 key（内容本来一样）。并发保存时只有 1 个会成功，
      # 其余 3 个报 "Cache already exists" 属正常噪音，不是故障
      - name: Restore Perl modules
        id: perl-cache
        uses: actions/cache/restore@v4
        with:
          path: /home/runner/perl5
          key: perl5-${{ runner.os }}-test-nginx

      - name: Install Test::Nginx
        id: perl-install
        if: steps.perl-cache.outputs.cache-hit != 'true'
        run: |
          set -eux
          PERL_ARCH="$(perl -MConfig -e 'print $Config{archname}')"
          export PERL5LIB="$HOME/perl5/lib/perl5:$HOME/perl5/lib/perl5/$PERL_ARCH"
          echo "PERL5LIB=$PERL5LIB" >> "$GITHUB_ENV"
          cpanm --notest --local-lib="$HOME/perl5" Test::Nginx IPC::Run \
            > build.log 2>&1 || (cat build.log && exit 1)
          perl -MTest::Nginx::Socket -e1

      - name: Export PERL5LIB
        if: steps.perl-cache.outputs.cache-hit == 'true'
        run: |
          set -eux
          PERL_ARCH="$(perl -MConfig -e 'print $Config{archname}')"
          echo "PERL5LIB=$HOME/perl5/lib/perl5:$HOME/perl5/lib/perl5/$PERL_ARCH" >> "$GITHUB_ENV"

      - name: Save Perl modules
        if: always() && steps.perl-install.outcome == 'success'
        uses: actions/cache/save@v4
        with:
          path: /home/runner/perl5
          key: perl5-${{ runner.os }}-test-nginx

      # ================= 运行 =================
      - name: Setup runtime env
        run: |
          set -eux
          echo "$OPENRESTY_PREFIX/nginx/sbin" >> "$GITHUB_PATH"
          echo "LD_LIBRARY_PATH=$OPENSSL_PREFIX/lib" >> "$GITHUB_ENV"

      - name: Report cache footprint
        run: |
          du -sh "$OPENSSL_PREFIX" 2>/dev/null || true
          du -sh "$OPENRESTY_PREFIX" 2>/dev/null || true

      - name: Run tests
        run: |
          set -eux
          prove -r -v t

      - name: Upload error logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: nginx-error-logs-ssl${{ matrix.ssl }}-or${{ matrix.or }}
          path: t/servroot/logs/
          if-no-files-found: ignore
